import Foundation

struct Constants {
    static let PACKAGE_NAME = "io.pigeonapp"
    static let USER_PREFERENCES_KEY_ANONYMOUS_UID = "\(PACKAGE_NAME).anonymous_uid"
    static let USER_PREFERENCES_KEY_BUILD = "\(PACKAGE_NAME).build"
}
